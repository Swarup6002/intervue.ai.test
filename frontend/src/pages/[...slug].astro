---
// ⚠️ THIS FILE IS DEPRECATED.
// We have moved the React App to src/pages/interview/[...slug].astro
// to prevent it from blocking your Home, Signin, and other static pages.
export function getStaticPaths() {
  return []; // Generate nothing from this file
}